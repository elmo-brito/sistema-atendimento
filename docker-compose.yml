version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - uploads:/app/uploads
    environment:
      - SECRET_KEY=minha-chave-secreta-em-desenvolvimento
      - DATABASE_URL=sqlite:///app.db
    restart: always

volumes:
  uploads:
